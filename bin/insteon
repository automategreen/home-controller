#!/usr/bin/env node

var program = require('commander');
var pkg = require('../package.json');
var version = pkg.version;

program
  .version(version)
  .command('link <gateway> <device ...>', 'links device(s) to a gateway')
  .command('map <gateway>', 'map the insteon network')
  .parse(process.argv);